import { GenderDirective } from './gender.directive';

describe('GenderDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderDirective();
    expect(directive).toBeTruthy();
  });
});
